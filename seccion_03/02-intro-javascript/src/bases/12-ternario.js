/*------------Operador condicional ternario------------*/

const activo = true;

const mensaje = (activo) ? 'Activo' : 'inactivo'; // if - else

const soloActivo = activo && 'Activo'; // if 

console.log(mensaje);
console.log(soloActivo);